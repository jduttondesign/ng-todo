"use strict";

var app = angular.module("TodoApp",["ngRoute"]); //add ngRoute inside [] as well as the added js file in index file. 
//This tells angular to use the file you've added in index. If you use another plug in google that ngRoute "name"